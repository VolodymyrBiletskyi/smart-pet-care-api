{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      // Put back what appsettings.json raises: seeing the generated SQL is the
      // point of running locally, and nothing here is filling a 7GB volume.
      "Microsoft.EntityFrameworkCore.Database.Command": "Information"
    }
  }
}
